"""
AIそれって本当？ ショー設定。

新しい企画を追加するときはこのファイルを参考にして
scripts/shows/{show_id}.py を作成し、shows/__init__.py の REGISTRY に追加する。
"""

import re

SHOW_ID   = "aitsm"
SHOW_NAME = "AIそれって本当？"
DAYS      = [0, 2, 4]  # 月・水・金（0=月, 2=水, 4=金）

# ── 会議プロンプトへの追加セクション ─────────────────────────────
# run_meeting.py が今日の曜日を判断して動的に MEETING_FORMAT へ追加する。

MEETING_INSTRUCTIONS = """
## AIそれって本当？テーマ選択

今日は「AIそれって本当？」の配信日です。以下を実行してください。

1. docs/aitsm_themes.md を Read toolで読み、`- [ ]` の未使用テーマ候補を確認する
2. 今日採用したnoteテーマとの相性、または単独で読者に刺さりやすいものを1つ選ぶ
3. 会議ログの末尾（改善アクションの後）に以下の形式で追記する。形式を変えないこと。

## AIそれって本当？テーマ
[ここにテーマ名のみ。例: 筋トレすると自己肯定感が上がる説]
"""

# ── 会議ログからテーマを抽出 ────────────────────────────────────

def parse_meeting_decision(meeting_text: str) -> dict | None:
    """会議ログから今日のテーマを抽出する。見つからなければ None。"""
    m = re.search(r"## AIそれって本当？テーマ\s*\n(.+)", meeting_text)
    if not m:
        return None
    theme = m.group(1).strip().strip("[]")
    if not theme or theme.startswith("ここに"):
        return None
    return {"theme": theme}

# ── コンテンツ生成プロンプト ──────────────────────────────────────

_PROMPT = """\
あなたは「AIそれって本当？」の番組制作チームです。
今日のテーマ「{THEME}」で、X投稿3本とnote記事を制作してください。

## 番組コンセプト

世の中の「それって本当なの？」を、役割の違うAI Agentたちが番組形式で検証する。
意見の違いがあり、根拠や例外が出て、暫定判定がある。

## 出演Agent

- **MC**（司会）: 落ち着いていてテンポを作る。今日の説を紹介し最後の判定につなげる。
- **現実派**: 実生活で成立するかを見る。「それ実際むずい」で切り込む。理想論を嫌う。
- **感情派**: 人の気持ちや心理面を重視する。理屈で説明できない部分を持ち込む。
- **分析派**: 議論を整理し条件を分ける。冷静で「誰にとって本当か」を言語化する。
- **逆張り**: 安易な結論を壊す。少し皮肉だが嫌味にならない。場を引き締める役。

## 暫定判定の形式（いずれか1つ）

- 本日の判定：かなり本当
- 本日の判定：条件付きで本当
- 本日の判定：半分本当
- 本日の判定：思ったほどではない
- 本日の判定：幻想でした

---

## X × note の役割設計（重要）

**X = 入口であり、短編本編**。Xだけ見ても満足できること。
**note = 深掘り・保存版**。Xで刺さった人がさらに読む場所。

外部リンク付き投稿はXのリーチが落ちやすいため、「続きはnoteで」だけを押し付けない。
X上でミニ番組として完結させた上で、必要な場合だけnoteを案内する。

今日の投稿は以下の3パターンから最も適切なものを選ぶ:

- **パターンA（X完結回）**: 3本でミニ番組として完結。note案内なし
- **パターンB（note連動回）**: 3本で要点を出しつつ、3本目末尾で「もっと読みたい人はnoteへ」
- **パターンC（note先行回）**: noteの中で最も刺さる一節を切り出してXに展開

テーマが深掘りしがいのある場合はB、Xだけで伝わる場合はA、noteが先に強い場合はCを選ぶ。

---

## 実行手順

1. 以下の仕様に従ってX投稿ファイルとnote記事ファイルをWrite toolで保存する
2. docs/aitsm_themes.md を Edit toolで更新する
   （`- [ ] {THEME}` → `- [x] {DATE} {THEME}` に変更し、「## 使用済み」末尾にも追記）

---

## X投稿ファイル（content/x_posts/{DATE}_aitsm.md）

### 共通ルール（全パターン）

- X上で単独で完結・満足できること
- 各投稿は短くテンポよく。発言は1〜3文
- 全員が同じ口調にならない
- ハッシュタグは各本末尾に5つ（#AI #AIそれって本当 を必ず含める）

### 1本目（朝）

- 「AIそれって本当？」タイトルを1行目に
- 今日の説を明示する
- Agentの強い発言を2〜3個（一番対立する組み合わせ）
- 判定か判定への誘いで締める

### 2本目（昼）

- 番組名不要。すぐ本題に入る
- 1本目と別の切り口・別のAgentの視点
- 一番刺さる発言か対立点を中心に
- パターンBなら末尾に「深掘りはnote→」（任意）

### 3本目（夜 / 117字以内）

- 暫定判定（「本日の判定：〇〇」）
- 一言まとめ
- **パターンB/Cのみ** note案内を末尾に追記。パターンAはnote案内しない
- URLは含めない（自動追加される）
- **117字以内厳守**

---

## note記事ファイル（content/note/{DATE}_aitsm.md）

**800〜1500字（初期目安: 1200字前後）**

1行目: `# AIそれって本当？「{THEME}」を検証した`（必ず `# ` を先頭に付けること）

本文構成:
1. 今日の説（なぜ取り上げるか。2〜3文）
2. Agent会議（全Agent登場。テンポの良い会話。各発言1〜3文）
3. 論点整理（分析派が「誰にとって本当か」を整理。3〜5行）
4. 本日の判定（暫定判定 + 一言まとめ）
5. まとめ（読者が自分ごと化できる1文）

### 冒頭の書き方
- 1文で説明を完結させず、段落を分けて読者を引き込む
- 「〜とよく言われる」→「でも本当に〜なのか」→「今回検証した」の流れで自然につなぐ
- 説明文っぽくしない。読者が「あるある」と感じる入り方を意識する

### Agent会議の流れ（この順番を守ること）
1. **感情派**が強めの共感・体験から入る
2. **現実派**がそれを切る・揺さぶる
3. **逆張り**がズラす・別の切り口を持ち込む
4. **分析派**が整理する（「誰にとって本当か」を条件分けする）
- 全員が最初からきれいに論点を出しすぎない。人間らしい揺れを残す

### 根拠の扱い
- 「研究では〜」「〜が確認されている」などの表現は出典なしでは使わない
- 根拠を示したいときは「〜と言われている」「目安としては〜くらい」など推測・経験則の形にとどめる

### まとめの書き方
- 優等生的な教訓で終わらせない
- 「〜かもしれない」「〜という感覚が〜する」など、余韻のある終わり方にする
- 読者が自分に引き寄せて考えられる1文にする

末尾にハッシュタグ5つ。
保存前に文字数を確認し、800〜1500字に収めること。

---

## 出力完了後

「AIそれって本当？生成完了: {THEME}（パターン: A/B/C）」と出力して終了すること。
"""


def build_prompt(theme: str, date_str: str) -> str:
    return _PROMPT.replace("{THEME}", theme).replace("{DATE}", date_str)
